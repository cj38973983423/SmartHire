# 📋 SmartHire — AI 智能招聘助手

> 简历项目描述文档 · 可直接用于简历编写

---

## 一、项目概况

| 项目 | 内容 |
|------|------|
| **项目名称** | SmartHire — AI 智能招聘助手 |
| **项目定位** | 基于大型语言模型的智能简历初筛系统，辅助 HR 快速从海量简历中筛选出高匹配度候选人 |
| **开发时间** | 2025 |
| **代码仓库** | https://github.com/cj38973983423/SmartHire |
| **项目状态** | ✅ MVP 已完成，可完整运行 |

---

## 二、项目背景与目标

传统招聘场景中，HR 面对大量简历需要逐份阅读、手动比对 JD，耗时耗力且容易遗漏优秀候选人。本项目旨在利用大语言模型的语义理解能力，实现**简历的自动解析、智能评分、分类筛选**全流程自动化，将 HR 从重复性劳动中解放出来。

**核心目标：**
- 简历上传后自动解析为结构化数据（姓名、技能、经验、教育等）
- 基于 JD 全文进行四维度 AI 匹配评分（技能 40% / 经验 30% / 教育 15% / 综合契合度 15%）
- ≥60 分自动归入待筛选区，<60 分进入淘汰库
- 支持批量操作：多文件/文件夹上传、批量 AI 评分
- 提供数据看板，直观展示招聘进度统计

---

## 三、技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **前端框架** | React 18 + TypeScript | 类型安全、组件化开发 |
| **构建工具** | Vite 5 | 极速 HMR，秒级热更新 |
| **UI 组件库** | Ant Design 5 | 企业级 UI，完善的表单/表格/布局 |
| **后端框架** | Python 3.11 + FastAPI | 异步高性能、自动生成 OpenAPI 文档 |
| **ORM** | SQLAlchemy 2.0 | 声明式模型、灵活查询 |
| **数据库** | SQLite | 零配置，本地开发即开即用 |
| **AI 引擎** | Hermes Agent (DeepSeek/OpenAI) | 本地 subprocess 调用 LLM，无需额外部署 |
| **文档解析** | PyPDF2 + python-docx | PDF 文本提取、DOCX 内容解析 |
| **版本控制** | Git + GitHub | CI 自动构建校验 |

---

## 四、核心功能

### 📤 简历批量上传
- 支持 PDF / DOCX 格式
- **多选上传**和**文件夹拖拽**两种模式
- 上传后自动提取文本内容，调用 LLM 解析为结构化数据（姓名、技能列表、工作年限、教育经历、简历摘要）
- 解析失败不影响上传，保证体验流畅

### 🧠 JD 职位管理
- 完整的 CRUD（创建、读取、更新、删除）
- 支持启/停控制（is_active 字段）
- 职位信息包含：名称、部门、地点、JD 全文、必备技能、加分项、经验要求、学历要求
- 分页列表 + 关键词搜索

### 🤖 AI 四维度综合评分
- **技能匹配度（40%）** — 候选人技能与 JD 要求的匹配程度
- **经验匹配度（30%）** — 工作经验与 JD 的契合度
- **教育背景匹配度（15%）** — 教育背景是否符合 JD 要求
- **综合契合度（15%）** — 整体素质与职位/公司的匹配度
- 评分结果持久化到 `resume_scores` 表，支持同一简历对不同 JD 的多条评分历史
- 支持**单份评分**和**批量评分**两种模式

### ⭐ 智能筛选（≥60 / <60）
- **待筛选区**：自动展示评分 ≥ 60 分的候选人，按分数降序排列
- **淘汰库**：自动归入评分 < 60 分的候选人
- HR 可人工复核并标记：**初步通过** / **淘汰**，支持添加备注

### 📊 数据看板
- 简历总数、各状态数量（待处理/已查看/初步通过/淘汰）
- 平均评分、JD 总数
- 一目了然的招聘进度总览

---

## 五、系统架构

```
┌─────────────────────────────────────────────────────┐
│                   用户浏览器                           │
│          React + Ant Design + TypeScript              │
├─────────────────────────────────────────────────────┤
│                    HTTP (REST API)                     │
├─────────────────────────────────────────────────────┤
│                   FastAPI 后端                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐    │
│  │ JD 路由   │  │ 简历路由  │  │ AI 分析路由      │    │
│  │ CRUD     │  │ CRUD+搜索 │  │ 评分/批量/历史   │    │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘    │
│       │             │                  │              │
│  ┌────▼─────────────▼──────────────────▼──────┐      │
│  │          SQLAlchemy ORM + SQLite            │      │
│  │  job_descriptions | resumes | resume_scores │      │
│  └─────────────────────────────────────────────┘      │
│                        │                              │
│  ┌─────────────────────▼──────────────────────┐      │
│  │        Hermes Agent (subprocess)            │      │
│  │  hermes chat -q "prompt" → JSON response   │      │
│  └─────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

### 设计亮点

| 设计决策 | 考量 |
|----------|------|
| **Hermes subprocess 调用** | 无需额外部署 AI 服务，本地 LLM 即装即用 |
| **Hermes YOLO 模式** | 跳过安全确认，适合自动化批量调用 |
| **重试+降级策略** | LLM 输出非结构化时自动重试，保证系统鲁棒性 |
| **resume_scores 多对多表** | 支持同一简历对不同 JD 的多条评分记录，便于横向对比 |
| **JSON 四维度评分详情** | 评分结果含子维度分数和理由，提供可解释的 AI 决策 |
| **文件+数据库双删除** | 删除简历同时清理源文件，避免磁盘浪费 |
| **SQLite 本地存储** | 零部署成本，开箱即用 |

---

## 六、关键技术挑战与解决方案

### 挑战 1：LLM 输出不稳定（非标准 JSON）
**问题**：LLM 有时会输出带多余文字、markdown 代码块或不完整的 JSON
**方案**：
- 正则提取第一个 `{...}` 结构再解析
- 首次失败后自动重试，提示更简短
- 兜底返回默认值，保证系统不崩溃

### 挑战 2：大量简历批量评分效率
**问题**：AI 评分单次耗时约 5-15 秒
**方案**：
- 前端选中多份简历后一键发起批量评分
- 后端串行处理，保证评分一致性
- 异步数据库写入，评分成功即时持久化

### 挑战 3：简历格式多样化
**问题**：PDF 格式多样（扫描件、加密、排版混乱）
**方案**：
- 仅文本提取（PyPDF2/pdfminer），不依赖 OCR
- 非支持格式自动过滤并返回错误信息
- 上传即提取，失败不影响落库

---

## 七、API 接口一览

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/resumes` | 上传单份简历 |
| POST | `/api/resumes/batch` | 批量上传简历 |
| GET | `/api/resumes` | 简历列表（搜索/筛选/排序/分页） |
| GET | `/api/resumes/{id}` | 简历详情 |
| PATCH | `/api/resumes/{id}/status` | 更新简历状态+备注 |
| DELETE | `/api/resumes/{id}` | 删除简历 |
| POST | `/api/jds` | 创建 JD |
| GET | `/api/jds` | JD 列表 |
| GET | `/api/jds/{id}` | JD 详情 |
| PUT | `/api/jds/{id}` | 更新 JD |
| DELETE | `/api/jds/{id}` | 删除 JD |
| POST | `/api/analyze/score` | AI 评分（单份） |
| POST | `/api/analyze/score-batch` | 批量 AI 评分 |
| GET | `/api/analyze/scores/{id}` | 评分历史 |
| GET | `/api/health` | 健康检查 |
| GET | `/api/stats` | 仪表盘统计 |

---

## 八、项目亮点总结（简历速写版）

> 以下内容可直接用于简历中的「项目经历」板块

```
SmartHire · AI 智能招聘助手
技术栈：React + TypeScript + Ant Design / FastAPI + SQLAlchemy / Hermes Agent (DeepSeek)

- 独立开发了基于大语言模型的简历初筛全栈应用，覆盖简历上传→AI解析→智能评分→筛选全流程
- 后端采用 FastAPI 实现 RESTful API，包含 16+ 个接口，支持分页、搜索、多维筛选
- 设计了基于 JD 全文的四维度 AI 评分算法（技能40%/经验30%/教育15%/综合15%），
  评分结果含子维度分数和理由，提供可解释的 AI 决策
- 实现了 Hermes Agent subprocess 调用模式，无需额外部署 AI 服务
- 设计了多对多评分记录表（resume_scores），支持同一简历对不同 JD 的历史评分对比
- 前端采用 React + TypeScript + Ant Design，支持批量文件/文件夹拖拽上传、
  多选批量评分、待筛选(≥60分)/淘汰库(<60分)智能分类
- 实现了 LLM 输出异常处理（正则提取+重试+降级），保证系统鲁棒性
- 完整支持 Git + GitHub CI 工作流，代码质量有保障
```

---

## 九、运行截图（可补充）

> 建议在本机启动后截取以下页面：
> 1. **数据看板** — 总览统计卡片
> 2. **JD 管理** — 新建/列表页
> 3. **批量上传** — 拖拽上传区域
> 4. **简历列表 + 评分** — 批量评分前后对比
> 5. **待筛选 vs 淘汰库** — 自动分类效果

---

*此文档供编写简历参考，可根据具体岗位方向（前端/后端/全栈/AI）选择性裁剪内容。*
